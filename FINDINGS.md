# Findings

## GitHub synchronization

- Repository: https://github.com/saitakarcesme/LocalBot (public).
- Scheduled every minute; commit only when project changes exist.
- Current snapshot adds engine, provider adapters, and agent tools.
- Source reviewed for embedded credentials; none found. Development snapshot, not a tested release.
- Verified remote master matches snapshot 991228e.
- Reviewed runtime HTTP server snapshot; no embedded credentials found.
- Excluded personal reference screenshot using local Git excludes; contains contact details and private content.
- Verified remote master matches server snapshot 5e001d4.
- Reviewed macOS package, data models and app controller snapshot; no embedded credentials found. Development snapshot, not a tested release.
- Verified remote master matches macOS controller snapshot ede580f.
- Reviewed main macOS conversation UI snapshot; no embedded credentials found. Development snapshot, not a tested release.
- Verified remote master matches conversation UI snapshot 154f3ea.
- Reviewed macOS settings, approval and activity views plus app packaging script; no embedded credentials found. Development snapshot, not a tested release.
- Verified remote master matches settings and packaging snapshot e81d1b3.
- Reviewed runtime regression tests, task insert correction, shell permission changes and SwiftUI fixes; no embedded credentials found. Snapshot only; tests not run by synchronization task.
- Verified remote master matches fixes and tests snapshot b197f23.
- Reviewed sandbox root access adjustment and pinned Node distribution packaging with checksum verification; no embedded credentials found. Snapshot only, not tested by synchronization task.
- Verified remote master matches Node packaging and sandbox snapshot 23a65d3.
- Reviewed task error reporting, recovery, shell failure propagation, regression tests, Keychain access and conversation styling updates; no embedded credentials found. Snapshot only; tests not run by synchronization task.
- Verified remote master matches error reporting and UI snapshot 0d544ca. Server changes arriving during push remain for the next cycle.
- Reviewed runtime process lock and HTTP, provider streaming, sandbox and cancellation test additions; no embedded credentials found. Snapshot only; tests not run by synchronization task.
- Verified remote master matches runtime lock and test snapshot b06d776.
- Reviewed architecture documentation for publication; no embedded credentials or private conversation data found. Documentation claims were not independently tested by synchronization task.
- Verified remote master matches architecture documentation snapshot b9f84d3. New smoke script remains for next cycle.
- Reviewed workspace-aware concurrency, group context, settings, icon generation and local-model smoke script; no embedded credentials found. Snapshot only; scripts and tests not run by synchronization task.
- Verified remote master matches concurrency and icon tooling snapshot cb2057c. Provider edits arriving during push remain for next cycle.
- Reviewed local Ollama startup, attachment previews, reconnection handling and provider formatting; no embedded credentials found. Snapshot only; tests not run by synchronization task.
- Verified remote master matches Ollama startup and preview snapshot 045c199.
- Reviewed credential endpoint scoping, tool-use verification, context limits, search serialization, approval scrolling and smoke script updates; no embedded credentials found. Snapshot only; tests not run by synchronization task.
- Verified remote master matches credential scoping and tool verification snapshot 14ac4bb.
- Reviewed README, remote model setup, UI specification and private screenshot ignore rule; no embedded credentials or private conversation contents found. Documentation claims not independently tested by synchronization task.
- Verified remote master matches documentation snapshot 5c509e9. Concurrent push caused a ref-lock rejection, but the same commit was already on GitHub; fetched remote tracking state. New development edits remain for next cycle.
- Reviewed source formatting, Prettier development dependency, removal of unverified model replies from context, and added tool/concurrency regression assertions. No embedded credentials found; tests not run by synchronization task.
- Also reviewed structured read_file output; verified remote master matches formatted source and regression snapshot f0227ce.
- Reviewed Git inspection regression test and read_file formatting; no embedded credentials found. Tests not run by synchronization task.
- Verified remote master matches Git inspection test snapshot e48f65a.
- Local filesystem responsiveness recovered. Reviewed scroll observer and four LocalBot screenshots containing test conversations and local settings; no credentials or personal contact data found. UI tests not run by synchronization task.
- Verified remote master matches scroll observer and screenshot snapshot 0909e17. Previous local I/O delay resolved.

## Local implementation verification

- Installed runnable native application at `~/Applications/LocalBot.app`; bundled runtime reconnects to persisted SQLite history.
- 22/22 automated tests pass. Real Ollama Qwen3 1.7B completed file write/read, shell verification and four-agent shared-model read test with correct replies.
- Fixed an observed SwiftUI scroll feedback loop and reverified search, appearance, resize and settings in the installed app.
- Final observed idle RSS: client about 82 MB, runtime about 25 MB; 47% system memory free, about 12 GiB disk available. Model unloaded after idle keep-alive.
- Architecture, remote setup, UI measurements and honest test limits are in `docs/`. PC/27B hardware remains unverified.
- Reviewed implementation verification report and README clarification for publication; no embedded credentials or private conversation data found. Test claims belong to the implementation task and were not rerun by synchronization task.
- Verified remote master matches release verification documentation snapshot 67d3d33.

## v0.2 — CLI subscription bridge

- User approved implementation and micro commit + push workflow.
- Paused previous GitHub heartbeat via Codex automation tool.
- Installed Codex 0.144.6 app-server supports stdio JSON-RPC. account/read verified chatgpt authentication without reading credentials.
- Added isolated transport with request IDs, cancellation, timeouts and child cleanup. Provider integration remains in progress.

## v0.2 live checkpoint

- Subscription-backed GPT completed all five agent-specific tasks and automatically selected a three-agent team for Focus Ledger. Real files and 5/5 project tests independently verified.
- Native client created a second project conversation without manual membership; automatic routing and prior project context worked.
- Runtime suite: 26 passing tests. MCP HTTP transport and per-agent connection controls shipped; vendor access is not implied by transport support.
- 269 Codex registry entries tracked honestly; 13 LocalBot built-ins. Full parity remains open.
- GitHub heartbeat is paused; this task commits and pushes each checkpoint.

## Tool coverage continuation — precise file edits

Previous goal turn was progress (installed v0.2, live agent/project work, commits and verified UI). Added edit_file with SHA-256 stale-read detection, unique exact matching, atomic replacement and artifact integration. Tests verify permissions, cancellation, symlink refusal, ambiguous/stale input, preserved executable bits, unchanged hard-link aliases and temporary-file cleanup. A test path expectation was corrected for macOS canonical /private/var paths; both new tests passed, alongside the prior 26 tests. Full Codex patch syntax remains open.

Live edit verification passed through the installed runtime using the ChatGPT subscription. Exact tool sequence: read_file, edit_file, read_file. Approval was scoped to the verification filename and exact replacement; disk content independently matched.

## Tool coverage continuation — persistent goals

Previous goal turn made verified progress: precise editing installed, live read/edit/read passed and changes pushed. Added create_goal/get_goal/update_goal with conversation isolation, one unfinished objective, immutable completed goals and exact-ID update guards. Goals and evidence survive restart and are visible in Activity. Goal mutations follow configured edit-approval policy. Codex native goals/tool suggestions are disabled in decision sessions so actions stay in the LocalBot pipeline. All 29 runtime tests and native build passed. Token budgets, usage accounting and autonomous scheduling are not implemented or claimed.

Real GPT goal lifecycle passed: create_goal → run_tests (5/5) → update_goal complete → get_goal. Native Activity visibly shows the objective, completion state, evidence and action timeline. No unattended scheduling or budget accounting is implied.

## Tool coverage continuation — process sessions

Previous checkpoint was verified progress (persistent goals and real 5/5 test evidence). Added process_start/process_poll/process_input/process_stop using the existing macOS sandbox. Ownership isolation, UTF-8, stdin/EOF, cancellation, lifetime/output/concurrency bounds, task cleanup, agent configuration revocation and nonzero exit reporting are tested. All 34 tests pass. Live installed subscription-backed GPT executed start → input → poll; expected output and exit 0 were independently checked and visible in the native conversation. No PTY or restart persistence is claimed. Built-in tools: 21; full 269-entry parity remains open.

## Project routing continuation

Previous turn was verified progress: sandbox process sessions installed, tested against GPT and pushed. Inspection found routing happened before task persistence and skipped subsequent queued prompts. Routing now runs inside each persisted task, shares cancellation and resource reservations, and selects the team independently for every automatic-project message. Conversation serialization and SQLite insertion order prevent future queued messages from leaking into earlier task context. Unit/integration suite: 36 passing tests, including queued team changes and cancelled routing with no runs or team mutations. Live installed GPT validation passed: two immediately accepted project messages routed to Mira and Robin respectively, each performed its own read_file action. Cancelling a separate task during routing preserved an empty team and produced no tool actions. Native inspection also exposed hidden sender labels after single-member rerouting; project messages now always display sender identity.

## MCP resource continuation

Previous turn was verified progress: per-task project routing and sender identities installed and tested. Added resource/template discovery and approved server-mediated reads with integration permission enforcement, pagination, capability checks and bounded content validation. Resource-only integrations now pass connection discovery. All 37 runtime tests and native build passed. Live GPT subscription verification passed against a temporary loopback-only MCP fixture: three real actions discovered resources/templates and read the approved brief. Mira correctly returned Cedar and seven records; native chat verified. Temporary integration and agent permission were removed. No external vendor validation is claimed. UTF-8 byte-size limits were also tested.

## Multi-file patch continuation

Previous turn was progress: resource discovery and reading verified with GPT and real MCP HTTP. Added strict multi-file apply_patch with stale-read SHA-256 checks, Add/Update/Delete/Move, exact anchors and EOF context. Preflight rejects malformed/ambiguous/stale/escaping plans before mutations. Recovery preimages are retained and published as artifacts; commit-time failures roll back earlier changes, with explicit manual-recovery reporting if external changes prevent safe rollback. Tests include CRLF, file modes, paths, permissions, cancellation and a forced second-file failure. 41/41 tests passed. Installed GPT performed read/read/apply_patch/read/read with one narrowly approved patch. Independent disk checks confirmed update, move, add and delete. Native chat displayed recovery.json and both resulting file artifacts. Full fuzzy parser compatibility and crash-atomic multi-file transactions are not claimed.

## Native conversation continuity

Previous turn was verified progress: multi-file patches tested with GPT and recovery artifacts. Fixed scroll-follow intent on macOS 15+: user scroll phases are distinguished from lazy content/attachment size changes, preserving reading position while Activity changes transcript width. Native screenshot/AX checks verified the final message and all three artifacts on open, scrolling up suspends following, Activity preserves that position, and the down button resumes following. Reopening the long conversation was also checked. Added last-selected conversation persistence; selected Mira, quit, relaunched and verified Mira remained selected. Native build/signature passed. Runtime unchanged (last suite: 41 passing tests). Legacy macOS 14 scroll fallback is unchanged and was not tested on hardware.

## Older conversation history

Previous turn was verified progress: native scroll and last-selection behavior installed and checked. Added cursor-based 300-message pages with conversation ownership validation, native Load earlier messages and merge behavior that preserves explicitly loaded older pages. All 42 runtime tests passed; native build/signature passed. A temporary 650-record system-message fixture exercised the installed app: pages 300+300+50 loaded, first record reached and load button removed at exhaustion. Screenshot verified record 0350 retained its screen position after prepending a page. This stress test exposed lazy measurement snapback, fixed by limiting automatic layout-following to viewport changes. The temporary fixture and its FTS entries were removed after test, leaving real chats/projects untouched.


Search navigation: 43/43 runtime tests and native build passed. In the installed app, searching a temporary 650-message fixture for record 0007 opened its bounded historical window and visibly highlighted the match. Latest messages returned to record 0649. The fixture and its FTS records were removed, preserving real conversations. Runtime checks reject cross-conversation cursors and conflicting before/through cursors. TypeScript compiler dependencies now use a lockfile-keyed cache under ~/Library/Caches/LocalBot/RuntimeBuild, avoiding observed iCloud file-open stalls.


Local MCP stdio: 49/49 runtime tests and native app build passed. Installed GPT subscription Mira executed three approved stdio actions: resource discovery, template discovery and reading the brief. The actual child returned Cedar/seven sample records; native conversation showed the result and three actions. Temporary server files, integration and agent grant were removed. Tests cover protocol negotiation, tool/resource calls, concurrent response correlation, UTF-8, missing executable, malformed/oversized output, environment isolation, cancellation, process cap and shutdown. Native Integrations form exposes executable/args/cwd and explains user-account access. OAuth, explicit environment-secret injection and third-party vendor validation remain open.


Runtime clock: 51/51 tests and native build passed. current_time provides UTC, Unix milliseconds and optional IANA local time/offset without filesystem, terminal or network access. Tests cover daylight-saving transition, previous-day conversion, invalid zones and cancellation. Installed subscription-backed Mira called the tool with Europe/Luxembourg and accurately reported 2026-09-16 14:28:01 +02:00. The recorded timestamp was independently bounded by smoke-test start/end times; the native conversation was inspected. Built-in tools: 26.


Native message formatting: added inline emphasis/code and fenced code blocks with horizontal scrolling and a copy-code button. User text remains literal, source messages remain unchanged, and whole-message Copy retains original text. Non-HTTP(S) Markdown links are inert. Foundation checks cover preserved code, longer/tilde/unclosed fences, line breaks, emphasis/code attributes, URL schemes and plain previews. Native build passed; live subscription-backed Alex generated a JavaScript greeting and the installed UI rendered it correctly. Copy code → paste into composer matched the code exactly; the test draft was cleared without sending. Sidebar/search previews now omit formatting markers. This is inline/code support, not a full Markdown document renderer; tables and heading layout remain plain text.


Process wait continuation: previous turn was verified native-formatting progress. process_poll now uses cancellable output/exit notifications with a bounded timeout (default 10 seconds, maximum 60 seconds, zero for immediate snapshots). Ownership, single pending waiter, cancellation, task cleanup, timeout and once-only output consumption are covered; all 53 runtime tests and app build passed. Installed subscription-backed Alex started an approved command with a 20-second delay and supplied stdin. First poll waited 14,342 ms and returned new output; second returned exit 0 in 3 ms with no duplicate output. Native conversation showed the correct result and four actions. No new PTY, detached-job persistence or longer process lifetime is claimed.


Image attachment continuation: previous turn was verified process-wait progress. Added provider-independent image references and Codex app-server localImage inputs, confirmed against the installed generated protocol schema and official App Server docs. 55/55 runtime tests and native build passed. Four images maximum, 5 MB each / 12 MB total; signatures and file types checked before model invocation. Installed subscription-backed Mira read only a generated fixture and correctly returned 7419, one blue circle and two orange squares, with zero tool calls. Prompt and filename contained none of these answers. Native screenshot verified attachment and response. HTTP providers still explicitly lack image inspection; arbitrary workspace view_image tooling and image generation remain open.


Workspace image tool: 56/56 tests and native build passed. view_image enforces filesystem-read permission and workspace/symlink confinement, validates image input limits, and supplies a retained artifact copy to the next model generation. Only image-capable providers advertise it. Live installed subscription Mira called view_image exactly once on a generated workspace fixture and correctly identified 7419, one blue circle and two orange squares. Native chat displayed the artifact and one action. The temporary workspace source was removed; the artifact remains. Initial test path expectation was corrected for macOS canonical /private/var paths. HTTP-provider vision and image generation remain open.


Local HTTP vision continuation: previous turn was verified workspace image inspection. Added opt-in imageInput capability for Ollama/OpenAI-compatible providers and native model settings, preserving Codex subscription use. Images are encoded as base64 or image_url data URLs; no local paths are transmitted. Supplementary image user messages follow all tool responses. 60/60 runtime tests and native build passed, including actual loopback HTTP payload/stream tests and settings persistence. Installed UI shows the option off for the existing qwen3:1.7b model. Ollama discovery confirmed that model advertises completion/tools/thinking, not vision; no large model was downloaded. Actual local vision inference and Anthropic vision remain unverified/unimplemented respectively.


Configurable agent run limits: previous turn was verified local HTTP vision progress. Contacts now set maxSteps from 1 to 256, defaulting to the legacy 24. Bounds are enforced in HTTP configuration and runtime; each participant uses its own limit at run start. Exceeding the limit preserves actions/checkpoint and fails explicitly rather than reporting completion. 62/62 tests and native build passed; controlled model tests prove 3-step failure versus 5-step completion. Live subscription Mira executed a clock call at limit 1, stopped with the correct error, then completed a follow-up after restoring its prior limit. Native contact form showed restored 24 and the chat showed both outcomes. Inspection exposed a stale eyes reaction on failure; now failed runs set warning, covered by the final test suite. This does not add unattended scheduling, token budgets or automatic run resumption.

### Conversation archive checkpoint — 2026-09-16
Reversible archive/restore now organizes multiple direct-agent and project chats without deleting history, FTS entries, artifacts, or project context. Runtime blocks archiving unfinished tasks; valid follow-up messages restore archived chats transactionally. Native context menus, archive-list switch, empty state, and selection synchronization implemented. Existing Mira UTC history round-tripped through native archive/restore successfully; 63/63 runtime tests and final native build pass. This is UI/runtime conversation management, not a claim that all Codex-host archive tool equivalents are implemented.

### Public web redirect checkpoint — 2026-09-16
Real research was unnecessarily blocked by all HTTP redirects being refused. Extracted a bounded HTTPS reader, revalidates and pins DNS on every hop, blocks special-use/transition IPs, caps redirects at five and total time at 20 seconds. Preserves split UTF-8 and plain-text angle brackets. 67 tests pass; installed Mira completed one actual redirected Wikipedia fetch through the Codex subscription bridge and produced a source-linked answer. Tool count stays 27; this improves web_fetch, not full web/browser parity.

### Question lifecycle checkpoint — 2026-09-16
Inspection found answered ask_user tasks stayed awaiting_input forever, preventing archiving. Follow-up enqueue now transactionally marks prior waiting tasks/runs continued and clears only their transient agent reactions; failed sends roll back. Waiting questions can be cancelled from the runtime and native Stop controls, with idempotent cancellation messages. Real subscription Mira answer/cancel/archive flows passed; native conversation observed, 68 runtime tests passed (18 core tests rerun after fixture cleanup). No replay of completed tool calls or in-place process resumption is claimed.

### Agent history retrieval checkpoint — 2026-09-16
Agents previously saw bounded recent context without a retrieval tool. Added search_history using existing SQLite FTS, scoped to the task's conversation/project and strictly before its user message. Source-identified bounded excerpts let agents recover older project decisions without loading the entire transcript or accessing unrelated chats. Real Mira search of Focus Ledger returned seven matches and a source-named explanation. Tests70/70, built-in tools28, installed native app verified. This is not complete Codex read_thread parity.

### Automatic project-context ordering checkpoint — 2026-09-16
Found the automatically injected sibling-project history lacked search_history's task-message cutoff. Queued work could therefore receive a later sibling instruction as history. Added bounded source-identified excerpts before the task's message and whole-entry budgeting. Provider-boundary regression gates routing to reproduce the timing; earlier evidence arrives, future sibling requests and unrelated private chats do not. Tests71/71, built and installed, signature verified. Shared durable project memory is not versioned by this change.

### Paginated agent history checkpoint — 2026-09-16
Added read_history to complement search_history: bounded chronological pages with source metadata and owned cursors, current/same-project scope, task-message cutoff and explicit long-message truncation. Test covers13 messages across3 pages, foreign/future cursor rejection and archived sources. Live Mira read2 messages from prior Focus Ledger summary and grounded its response in that tool output. Tests72/72, built-in tools29, app installed; full unbounded message/attachment reads remain outside this tool.

### Complete history-message chunks checkpoint — 2026-09-16
Closed read_history's long-message gap: bounded single-message chunks with Unicode offsets and nextOffset now allow complete historical text retrieval. Scope and task-time boundary are rechecked for every chunk. Three-chunk Unicode reconstruction and invalid cursor/offset cases tested; live subscription Mira source chunk matched persisted text exactly. Tests73/73; installed app;29 tools unchanged. Attachment bodies remain outside history retrieval.

### Native project editing checkpoint — 2026-09-16
Projects now have an editor for name, workspace and shared notes. Runtime validates folders, rejects unfinished project work, and compares the original fields transactionally to prevent stale UI overwriting newer memory.75 tests pass. Native rename/save/reopen/restore verified on Focus Ledger with history/folder/notes preserved. Context-menu access and header spacing improved. Workspace changes select future work location; no files are moved.

### Shell credential-path checkpoint — 2026-09-16
Found direct file tools and shell sandbox disagreed on protected credential paths. Shared case-insensitive credential definitions now cover both; real sandbox cat/redirection tests reject13 generated credential fixtures while normal workspace operations pass. Corrected SBPL regex escaping based on failing regression before shipping.76 tests pass; built, signed, installed and reopened. Git metadata exception documented; no claim of arbitrary secret-content detection.

### Subscription-backed web search checkpoint — 2026-09-16
Added the previously missing actual web search through an optional provider capability, currently Codex CLI subscription only. Separate ephemeral search worker returns observed webSearch actions plus model summary/source links through LocalBot Activity and Web permission enforcement. No API key or silent local-to-cloud fallback. Real CLI and installed Mira search both returned SQLite official documentation; native response verified.78 tests pass,30 built-in tools; interactive browser and other web categories remain incomplete.

### Source-link verification correction — 2026-09-16
Baseline comparison disproved the assumption that bare URLs lacked native detection: Foundation already links them. Removed redundant NSDataDetector code and retained only credential-bearing URL link suppression plus focused formatting regressions. Swift checks pass; runtime unchanged. Native UI automation unavailable this continuation, so installed-app replacement/live click verification remains pending rather than claimed complete.

### Search event integrity checkpoint — 2026-09-16
Subscription web search now requires an observed search action, not merely openPage/findInPage. Validated action shapes and a20KB aggregate event budget prevent malformed/oversized Activity payloads; returned summary/source fields are explicitly projected.78 full runtime tests pass, plus focused positive multi-query/open/find and negative malformed/open-only/oversized/cancellation regressions. A real authenticated Codex CLI search returned the official SQLite WAL source with a search event. Native package build and ad-hoc signing passed; UI automation remains unavailable, so no installation or live click claim.

### Project-aware automatic routing — 2026-09-16
Previous checkpoint was progress: committed search-event validation and verified real CLI search. Inspection now found that project context reached executing agents but not the team organizer. The organizer now receives project name, up to4000 characters of shared notes, and bounded source-identified prior sibling history using the same task cutoff as agent execution. Shared history serialization is reused rather than duplicated.79 runtime tests pass, including queued routing with earlier decisions, unrelated-chat exclusion and future-message exclusion. Native package build/signing passed; isolated real subscription scenario passed: a context-only Turkish request selected coder then reviewer, coder wrote focus.json with dailyMinutes25 and reviewer read the actual file; independent JSON assertion verified the artifact. User conversations were untouched. Native installation remains pending UI tool availability.

### Local direct-chat titles — 2026-09-16
Previous turn progressed project-aware routing with a verified real subscription workflow. Inspection found non-Codex direct chats skipped title creation entirely. Local/HTTP direct chats now use a whitespace-normalized first-request preview (80 Unicode code points maximum), prefixed with the contact name outside projects. Attachment-only requests use the attachment name. This costs no extra inference; Codex retains semantic model-generated titles. Follow-ups and manually titled conversations remain stable.80/80 runtime tests pass, including inference-count, team-preservation, manual-title and emoji truncation assertions; native package build/signing passed. Native UI automation is still unavailable, so installation/live sidebar verification remains pending.

### Task-scoped conversation history — 2026-09-16
Previous turn progressed local chat title behavior. Found that execution/routing filtered future tasks only after the UI's last300-message window, allowing later messages to evict relevant history; unowned later messages also passed through. Added Store.taskMessages with SQL eligibility before LIMIT, used by both organizer and executing agents. Earlier/current-task outputs remain eligible for team handoffs, while unowned messages stop at the current user message.81 runtime tests pass, including350 intervening later messages and a queued future task at the actual provider boundary. Native package build/signing passed; real subscription handoff regression passed: coder wrote the planned JSON artifact, reviewer read it, both runs completed, and an independent assertion verified file contents. UI installation remains pending.

### Agent directory tool — 2026-09-16
Previous turn made verified history-isolation progress. Added list_agents:20-contact keyset pages with IDs, names, roles, configured permissions and current-conversation membership. Explicit projection excludes private prompts, memory, provider settings and workspace paths. No delegation or membership mutation.82 tests pass, including actual model-to-tool dispatch with all filesystem/network/terminal permissions disabled,27-contact pagination, field boundaries and fresh permission updates. Real Codex subscription Mira called the tool and correctly identified Alex/Mira/Sam. Native package build/signing passed; installation remains pending UI automation. Built-in count31; refreshed the inventory's stale built-in list from runtime definitions without changing269 target entries or claiming collaboration-tool parity.

### Visible restart recovery — 2026-09-16
Previous turn made verified directory-tool progress. Recovery already marked unfinished work interrupted but left active eyes reactions and gave no conversation notice. Recovery now atomically adds one system notice per interrupted task, converts only active-run eyes to warning, expires approvals and timestamps interrupted rows. Completed tool results, completed-agent/user reactions, queued tasks and awaiting-input questions are preserved. Expanded disk-reopen regression proves idempotence across another reopen.82 tests and native package build/signing pass. This does not replay actions or automatically resume interrupted work; live installed UI verification remains pending unavailable native automation.

### Follow-up queued before a question — 2026-09-16
Previous turn progressed restart recovery. Found a race: enqueue clears existing questions, but a preceding run can ask its question after the follow-up was queued. The follow-up then completed while the previous task stayed awaiting_input and blocked archiving. Starting a queued run now transactionally continues earlier waiting questions before marking the new task running. Controlled provider-boundary regression gates the first generation, queues the answer, then emits ask_user: the first team stops after coder, follow-up coder/reviewer see the question and answer, stale warning clears, and archiving succeeds.83 tests pass after correcting SQLite row-prototype comparison in the new assertion. Native package build/signing passed; live UI remains unverified.

### Per-agent group outcomes — 2026-09-16
Previous turn progressed queued-question handling. Found task-wide hadErrors also controlled each agent's completion reaction, so an earlier failure contaminated successful teammates; run rows always said completed. Each run now tracks its own tool errors and stores completed_with_errors when appropriate, while task status still aggregates all failures. Group handoff evidence includes observed failed calls with explicit status labels.84 tests pass: a real missing-file tool failure followed by a successful clock call yields coder warning/reviewer success, and failed evidence reaches the reviewer model input. Corrected an older test that expected completed for two permission-denied runs. Native build/signing passed; live installed UI verification remains pending.

### Bounded team evidence — 2026-09-16
Previous checkpoint progressed per-agent outcomes. Group handoffs still loaded every complete tool output before slicing each item, leaving total model input unbounded. Store.taskEvidence now queries at most20 recent observed calls with SQL output excerpts, then applies a3000–8000 character runtime handoff budget. Call IDs/statuses and explicit per-output truncation/omitted-result counts preserve transparency; full persisted Activity is unchanged.85 tests pass, including40 outputs of40KB, latest-failure visibility,5000-character total bound, unrelated-task exclusion and original-output preservation. Native build/signing passed. This bounds the handoff section, not an exact tokenizer budget for the entire prompt; native installation/live UI remains pending.

### Agent-readable Activity — 2026-09-16
Previous turn bounded team evidence. Added read_activity so agents can recover omitted current-task observations without replay: five-action pages and2000-Unicode-character output chunks with cursors, statuses and source call IDs. Scope checks reject other tasks and recursive activity-reader results.86 tests pass, covering model-to-tool dispatch, one clock call followed by saved-output reads, full Unicode reconstruction, pagination, bounds and cross-task rejection. Native build/signing passed; real subscription smoke passed with exactly one current_time call and two read_activity calls; the chunk matched the original output exactly. Built-in count32; full269-entry parity remains incomplete.

### Denied-action repeat suppression — 2026-09-16
Previous checkpoint progressed recorded Activity retrieval. Model instructions discouraged retries after denial, but runtime still showed another approval for identical calls. Each task now remembers denied workspace/tool/argument combinations across its team, normalizing top-level argument ordering, and fails matching retries before asking again. A new user task starts with a fresh approval decision.87 tests pass: two agents make four attempts with reordered arguments, only one approval is created, all calls fail, no file/artifact is created, and a follow-up can request approval again. This is exact-call suppression, not semantic detection of rewritten commands or an expanded permission policy. Native build/signing passed; installation/live UI remains pending.

### Packaged runtime subscription verification — 2026-09-16
Previous turn progressed denied-action handling. Added an opt-in packaged smoke that executes build/LocalBot.app's bundled Node and server (not runtime/dist imports) against an isolated data directory and random localhost port. Verified unauthenticated401,0600 connection metadata, real Codex subscription task with one clock call/two Activity reads and exact saved-output equality. Stopped/restarted the packaged server and compared full message history, completed task status and action count; all preserved without replay. Both child lifecycles were awaited and stopped. Exposed npm run test:packaged-subscription and documented existing-login/usage requirement. Runtime suite remains87 passing from prior checkpoint; no runtime production code changed. Native app installation/window testing still pending unavailable UI automation.

### Runtime instance-aware native refresh — 2026-09-16
Previous checkpoint verified the app-bundled runtime. Native polling keyed solely on revision, which can collide across a fast restart without a failed HTTP request. Each server boot now supplies a fresh instanceId; Swift's RuntimeSnapshotCursor distinguishes unchanged snapshots, ordinary updates and restarts. Restart refresh also restores credentials through the existing noninteractive Keychain path. Optional decoding keeps legacy-runtime compatibility. Swift cursor scenarios and87 runtime tests passed; native build/signing passed. Packaged real subscription/restart smoke passed: different instance IDs with equal revisions, one clock call/two Activity reads and preserved history after another restart. Native UI automation remains unavailable, so installed-window behavior is not claimed verified.

### Retry-safe message submission — 2026-09-16
Previous checkpoint progressed restart-aware native refresh. Native sends previously had no request identity, so a lost HTTP response could cause duplicate work on retry. Client now retains a per-conversation request UUID plus content/attachment fingerprint until acknowledgement; edited payloads receive new IDs. Runtime stores the accepted request-to-task mapping transactionally with message/attachment/task writes, returns the prior task for exact retries, and rejects changed payload reuse. Removed redundant HTTP question-continuation side effects already handled transactionally by enqueue.88 runtime tests and Swift pending-send checks passed, including attachment retry, persisted mapping reopen, question-state preservation, failed-enqueue rollback and distinct fresh requests. Native build/signing passed; extended packaged subscription retry/restart smoke passed: immediate and post-restart retries returned the same task, one clock call/two Activity reads, exact history retained without duplicate messages. Installed UI remains pending.

### Non-blocking direct-chat naming — 2026-09-16
Previous turn progressed retry-safe submission. Direct Codex conversations previously failed the entire task if title organization returned malformed/missing metadata or errored. Direct/manual conversations now fall back to the existing bounded request preview and continue actual work; automatic routing errors and user cancellation still stop before starting agents. Extracted the shared preview path and injectable provider factory for meaningful failure tests; production provider selection defaults unchanged.89 tests passed, covering malformed/missing/throwing title responses with successful actual clock execution, automatic-team failure and cancellation. Native build/signing and the packaged real subscription happy-path regression passed, including saved-output reads, restart and retry deduplication. Metadata failures were injected in controlled tests; installed UI remains pending.

### Native work indicators and stop targeting — 2026-09-16
Previous checkpoint progressed direct-title fallback. Native activeTask previously chose the newest active row, so a newer queued message could mask the running task and misdirect Stop/typing identity. Running/approval tasks now take priority; queued-only selection uses the oldest available row. ConversationProgress distinguishes sending, queued, preparing, approval, real-run typing and hidden states. Avatar lookup uses agent ID rather than display-name equality, and sending status is scoped to its conversation. Swift state scenarios passed. Initial integrated SwiftUI build failed type checking; extracting the status row and correcting the existing integer automatic flag resolved it. Final native build/signing passed. Runtime code unchanged;89-test baseline retained. Live installed UI remains unverified while native automation is unavailable.

### Unfinished tasks remain visible — 2026-09-16
Previous checkpoint corrected native active-task selection. Runtime snapshots still limited the entire task list to the newest200, hiding older running/approval/input/queued work and undermining those controls. Snapshot now includes every unfinished task plus only the latest200 terminal task records. A495-task SQLite fixture verifies444 returned rows, old unfinished states, full queued visibility, no duplicates, active-run/task consistency and terminal-history bounds before/after reopen.90 tests and native build/signing pass. This scales unfinished metadata with the live backlog while keeping terminal history bounded; native installation/live UI remains pending.

### Agent task-status inspection — 2026-09-16
Previous checkpoint kept unfinished UI tasks visible. Added list_tasks with five-row pages, conversation/project scope, all/active filters, source conversation/message IDs and bounded excerpts. SQL includes the requesting task and earlier task rows only; foreign/future cursors reject, and no task state changes occur.91 tests pass, covering actual engine dispatch,13-task pagination, waiting-state preservation and privacy/cutoff boundaries. Real subscription Mira invoked list_tasks once and reported the observed running state from its result. Native build/signing passed.33 built-ins; refreshed inventory list without claiming full Codex-host tool parity. Installed native UI remains pending.

### Tool inventory consistency gate — 2026-09-16
Previous checkpoint added verified task listing. Added npm run audit:tools and generated TOOL-COVERAGE.md, now checked by npm test. Gate validates unique target/runtime names,269 target count, exact built-in synchronization, closed object schemas, descriptions, permission-dispatch reachability and unknown-tool denial; implemented/partial labels require route notes. It does not execute tools or certify enforcement/parity. Corrected view_image from full to partial because its own route lists unsupported resolution controls/unverified local inference. Current labels:4 implemented,9 partial,7 absent,30 LocalBot counterparts and219 service/auth adapters.91 tests plus inventory gate passed. Production runtime/native code unchanged; installed UI still pending.

### Contact edit conflict protection — 2026-09-16
Previous checkpoint added inventory consistency gates. Native contact editing could overwrite memory/permission changes made after the sheet opened. AgentEditor now retains its original value; existing-contact saves send it as expected state. Server compares normalized values and commits atomically, rejecting stale saves before releasing agent process sessions. Fresh edits and new contacts retain their existing behavior; callers omitting expected remain backward-compatible and do not receive conflict protection.92 tests plus inventory gate pass, including real HTTP stale-memory/permission preservation, fresh save and optional-field normalization. Native build/signing passed; actual contact-sheet interaction remains unverified without native UI automation.

### Signed build identity and installation preparation — 2026-09-16
Previous turn progressed contact conflict protection. Rechecked installed app: LocalBot PID693/runtime846 were live, with zero unfinished tasks. Native CUA controls remain unavailable; requested that the user quit LocalBot, with no answer yet. Did not replace running application files. Added build-info metadata and external signed-file hash manifests. Initial pre-sign hashes correctly failed after signing; moved full hashing after signing. Strict signature verification then exposed FinderInfo/file-provider metadata reappearing under synced Documents. Bundles now assemble in ~/Library/Caches/LocalBot/AppBuild (override LOCALBOT_APP_BUILD_DIR), with consistent defaults in package smoke/verifier. Cached build passed strict deep codesign verification and27 file hashes. Clean commit7b19ae1 rebuilt with sourceDirty=false; strict signature,27-file manifest and actual subscription packaged regression all passed in the cache location. App PID693 still live afterward, so replacement waits for the requested quit. No runtime code changes;92-test baseline retained.

### Guarded native installer — 2026-09-16
Added install:app with read-only --check, strict signature/full manifest verification, exact bundled executable process guards, staged copy, preserved prior bundle and rollback on failed promotion/verification. Source/destination identity and symlink guards prevent installing over the build itself or following an app symlink. No process termination, UI bypass, launch or user-data writes. Actual readiness check verified the cached source and correctly refused live native PID693/runtime846; no installation occurred. Successful promotion and rollback paths remain unverified on a closed native app.92 runtime test baseline is unchanged; this checkpoint only changes packaging scripts/docs. User quit response remains pending.

### Capability-aware automatic teams — 2026-09-16
Previous checkpoint added guarded installation. Automatic organizer saw only contact roles/names, so it could choose a contact whose permissions/provider excluded the requested tool. It now receives tool names from the same availability function used by worker execution plus autonomy. No contact memory, prompts or secrets are included in this catalog. Routing instructions distinguish configured capabilities from successful authentication/approvals.93 tests and inventory gate passed after correcting a syntax error in the new regression. The regression routes a write to the only writer despite another contact's Developer role, verifies exact written content, runtime tool-list equality, no private-memory leak and unchanged permissions. Real Codex subscription test with all five contacts labeled Developer selected only researcher (the sole write-enabled contact), executed write_file and produced the independently verified focus.json. This improves model selection evidence, not a guarantee of perfect routing; runtime permission enforcement remains authoritative. Installed native app PID693/runtime846 remain live; no replacement performed.

Capability-routing delivery check: clean commit2bd1068 built successfully into the cache bundle; strict deep signature verification and all27 signed file hashes passed with sourceDirty=false. Packaged real-subscription regression also passed (clock/read_activity, same-revision restart identity, persisted history and message retry deduplication). The currently installed app remains unchanged pending closure.

### Workspace-bound action approval — 2026-09-16
Previous checkpoint advanced capability-aware routing. Inspection confirmed per-call permission rechecks but exposed workspace drift: changing a contact folder while a tool waited for approval could execute the approved relative path in the new folder. Worker execution now pins the effective workspace from run start, checks live contact workspace before approval and after approval, and shows the workspace in the approval summary. A mismatch fails the action and requires a new request; project workspace overrides remain authoritative.94 tests plus inventory gate passed. Regression changes the contact folder during pending write approval, proves neither folder received a file/no artifact exists, then verifies a fresh request and fresh approval write successfully in the new folder. Native installation remains pending the previously requested app closure.

Workspace-approval package check: clean commitaadb8f9 built successfully, strict deep codesign verification and27-file manifest passed (sourceDirty=false). Native PID693/runtime846 are still live, so no installed files were replaced. Live subscription behavior was verified at the preceding capability-routing checkpoint; this approval race was exercised with a controlled provider and real filesystem writes in the94-test suite.

### MCP approval configuration binding — 2026-09-16
Previous checkpoint pinned action workspaces. MCP dispatch also reread the connection after waiting for approval without comparing it with the displayed configuration, allowing changed executable arguments or endpoint to inherit the old approval. Engine now requires deep equality between the originally authorized configuration and the freshly permission-checked connection before creating the MCP client.95 tests and inventory gate pass. A real child-process regression changes the configured program during pending approval: the first action fails and no startup marker exists; a fresh request/approval starts the replacement server and completes tools/list. This checks process launch, not merely an error string. No credentials are included in comparisons or logs.

MCP-approval package check: clean commit71f5500 built successfully; strict deep signature verification and27 signed-file hashes passed. Installed native PID693/runtime846 still live; no installation attempted. Native approval-sheet interaction remains unverified without the native control tool.

### Live tool availability between model steps — 2026-09-16
Previous checkpoint bound MCP approvals to their connection. Runtime enforced live permissions but advertised a fixed tool list throughout each run, causing agents to keep planning unavailable actions or miss newly enabled capabilities. Each generation now receives tools recomputed from the latest contact permissions/integrations, with provider capabilities pinned to the actual model configuration executing that run.96 tests and inventory gate passed. Regression revokes write permission after generation: the stale write call fails and produces no file; subsequent request omits write/terminal. Restoring write permission exposes it on the next step and creates the verified file. Changing the contact's provider to Codex mid-run does not advertise Codex search/vision to the already-running local provider. No external model calls in this controlled timing regression.

Live-tool package check: clean commitdcd7f98 built successfully; strict deep codesign and27 signed-file hashes passed. No native installation or interaction performed; existing installed application remains unchanged.

### Attachment-aware project organization — 2026-09-16
Previous checkpoint refreshed live tools. Organizer ignored attachments, so attachment-only messages provided an empty prompt with no file context. Routing now includes current-message filename/MIME/size metadata (16 entries, bounded names/types, total attachment count), without storage paths or other conversations' attachments. Instructions treat metadata as untrusted and require clarification when intent is unclear.97 tests and inventory gate passed after correcting a fixture SQL placeholder count. Scoped regression verifies filename/type/size, no storage path or unrelated-file leak, worker receipt of actual text and awaiting_input outcome. Real Codex subscription smoke submitted only requirements.txt: generated a contextual title, selected coder, executed list_files/read_file and ask_user, and retained awaiting_input with no invented artifact.

Attachment-routing package check: clean commit57030f3 built successfully; strict deep signature validation and27-file signed manifest passed. No installation or native UI interaction performed.

### Installed native handoff — 2026-09-16
User explicitly requested a bounded handoff for personal use, not resumption of the broad autonomous goal. CUA became available. Authenticated snapshot confirmed no queued/running/approval tasks; quit native LocalBot through CUA. Detached installed runtime846 remained: verified authenticated health PID, exact executable and server argv, rechecked idle state, then sent SIGTERM. Guarded installer successfully installed clean source57030f3 with strict signature/27-file verification and preserved prior bundle as ~/Applications/LocalBot.backup-ea1b59a9-39f8-4c17-b886-1f1af81b114e.app. Reopened the installed application via CUA; existing history and selection survived.
Native UI checks: switched conversations; created direct Mira chat; sent actual current_time request; saw contextual title, avatar/typing dots, eye reaction then completion check below the bubble. Opened Activity and verified Europe/Luxembourg input and actual runtime clock output. Created a Focus Ledger project conversation without manually selecting agents; automatic routing chose Sam. Sent a read-only request to inspect focus.cjs/tests and execute tests. Approved exactly the displayed run_tests command in the native UI; runtime completed list_files, two read_file calls and run_tests (exit0,5/5 passed), then displayed the final result. Clicking the project section header preserved conversation selection. App left open on the completed project result. Broad269-tool parity remains incomplete; no further goal work was started. Existing97-test baseline retained; this turn verified the installed build, not a new runtime change.

### September17 targeted project reliability and UI redesign
User requested race-game failure diagnosis, compact title-only chats below collapsible projects, consecutive-message grouping and live Activity. Failed task76ddce77 wrote Desktop/Projects/game/index.html (10361 bytes) successfully, then the next Codex generation hit its configured180s deadline. Existing CLI reasoning setting is medium, so high inherited effort was not established as the cause. Codex timeout errors now explain preserved outputs and recovery; failed/cancelled runs attach orphaned generated files to an explicit incomplete-verification message. New Codex connection default900s (existing values remain configurable). Engine publishes throttled safe phase labels via run_progress from real provider notifications, without exposing reasoning text.98 runtime tests passed including phase delivery and artifact visibility after a subsequent generation failure. Native sidebar/message and Activity stages compile and are microcommitted; final installed visual verification and real same-prompt game test are in progress. Protocol reference: https://learn.chatgpt.com/docs/app-server .

### 2026-09-17 — Recover malformed CLI decisions
- Live exact-prompt game run wrote HTML and executed tests, then failed on malformed nested JSON. Codex provider now retries one invalid JSON decision with explicit correction feedback; validates all calls before returning any for execution. Completed actions are preserved in context, permission/transport errors are not retried.
- 99 runtime tests pass, including bounded format retry and non-retry for permission errors.
- Original user game (Neon Apex) was opened in the browser: canvas renders, race distance advances, collision ends race, Race Again resets the run. Original source preserved.

### 2026-09-17 — Existing project recovery
- Installed CLI build completed the user's original game follow-up through native composer: read_file + approved run_tests, exit 0, syntax and start/collision/end/restart source checks all OK. This is source validation; actual start/collision/restart browser interaction was checked separately.
- Added idempotent recovery for historical orphaned attachments on terminal runs. Existing content and task status are not rewritten. Reopening the store twice is covered by the test.

### 2026-09-17 — Bound CLI output generation
- The second isolated exact-prompt test hit its 15-minute overall deadline while the model kept streaming a single decision, with no HTML saved. This test failed; it is not counted as successful project delivery.
- Added a CLI decision character budget derived from generation settings (16–64 KB), enforced on streamed deltas and final output. Oversized, unexecuted decisions get one smaller-step retry; completed actions are not replayed. Prompt requests a compact working first version before optional polish.
- Smoke configuration now matches the installed 4,000-token setting. 100 runtime tests and tool inventory gate pass. New live validation is pending.
- Native project disclosure collapse/expand, selection preservation, direct/project chat navigation, grouped messages, live phase/elapsed/approval, inspectable command output, and recovered HTML attachment verified in installed build 05cfbce.

### 2026-09-17 — Final bounded handoff
- Exact original one-sentence prompt now completes from an empty workspace in 109.285 seconds with authenticated gpt-5.6-sol through Codex CLI: list_files, write_file, read_file, run_tests; 8,680-byte single index.html; JavaScript syntax OK, task completed. Browser separately verified rendering, start, score progression, collision and restart.
- Installed signed source bb03d35; verified all 27 manifest files. Provider timeout 900s survived relaunch; no active installed tasks. Temporary browser and two test HTTP servers closed.
- Native folder selection, collapse/expand, selection retention and persisted collapse state verified after restart. Activity shows live agent phase, elapsed time, approvals, action details/output and saved files. Original user game verification completed without rewriting its source.
- 100 runtime tests pass. All code microcommits pushed. This is a bounded release handoff, not completion or restart of the broad goal/tool-parity roadmap.

### 2026-09-17 — Freeze, empty project layout and team handoff fixes
- Captured the reported freeze: native client consumed ~100% CPU while runtime was idle and responsive. Main-thread sample remained inside SwiftUI lazy transcript placement/graph updates. Replaced lazy transcript layout with a stable stack (message retrieval remains paged), removed synchronous scrolling from geometry callbacks, and separated the centered empty state from the transcript.
- Typing and message avatars now both use 25pt size, 24pt gutter and 7pt spacing. Empty automatic project conversations explain team selection.
- Avoid publishing unchanged connection state on every one-second poll; unchanged runtime snapshots no longer invalidate the whole UI.
- Each agent receives the ordered team, tool capabilities and later teammates. Role-specific execution replaces the instruction for every agent to carry out the entire request. Researchers hand off to the writable developer without asking to widen their own permissions.
- 101 runtime tests passed. Real gpt-5.6-sol CLI test completed Mira -> Alex: read-only research, then a 2,850-character HTML file written and checked by Alex. No permission escalation or user question was needed.
- Installed signed source 7578f66, all 27 bundle files verified. Native empty project chat, original transcript navigation, scrolling and Activity open/close checked; user history retained. One empty project conversation was created for UI verification and remains available.
- Final native idle measurement: 0.4% CPU over five seconds in the original conversation, down from the captured sustained ~100% freeze.

### 2026-09-17 — Automatic compose, inline activity and attachment service hang
- Compose toolbar, Cmd-N and project New conversation now create an empty automatic conversation directly. Runtime accepts an empty automatic direct team; routing selects capable agents and content-only titles in the prompt language. Live authenticated GPT CLI verification chose assistant, called current_time and titled the chat “Güncel UTC Saati”.
- Real tool events are interleaved with message timestamps. Pending/running actions animate, terminal/test output updates are throttled to 500 ms with bounded previews, and expanded rows show readable code and outputs. Activity inspector reuses compact rows.
- Reproduced the + freeze after replacing runModal: sample /tmp/localbot-plus-new-sample.txt shows main thread blocked inside NSOpenPanel.init / NSSavePanel bridge service. Replaced attachment OpenPanel entirely with a SwiftUI folder/file picker; folder listing and file reading run off the main actor. Verified native + opens, path selection and a 32-byte test attachment imports into the draft. Other legacy folder-chooser buttons still use system panels and may encounter the same OS service issue.
- Startup shows a native LocalBot launch/loading view until first snapshot, with reconnect status and retry. Fast launches may show it only briefly; no artificial delay.
- Always allow persists an exact action for one agent/workspace and can be revoked from agent settings. Full access — workspace skips individual approvals for enabled built-ins; disabled permissions, workspace sandbox and integration approval gates remain. No existing user permission configuration was widened. This is not unrestricted host access.
- 105 runtime tests pass plus the inventory consistency gate. Signed installed source 0e47709, clean manifest with 27 files. Existing history/project files and subscription authentication preserved. Broad goal loop was not resumed.

### 2026-09-17 — Project rows, attached activity and mythology identities
- Project chats show 30pt contact/stacked avatars, title and two-line preview; Recents remain title-only. Tool calls are grouped under their originating assistant message in a compact expandable component. Running work uses a reduced-motion-aware shimmer; expanded output is bounded, selectable, wrapped and updates from actual runtime events.
- Links use native NSTextView with per-link hover underline and pointing-hand cursor. Fixed a visual regression caused by SwiftUI width probes mutating the displayed text container; separate measurement layout now preserves readable widths. Native screenshot confirmed text, bold formatting and links. Automated pointer-hover verification was not available in the native control API.
- New projects can omit a folder: runtime allocates a fresh child in ~/Documents/LocalBot, handles same-name collisions atomically and sanitizes folder names. Existing-folder selection uses the nonblocking SwiftUI browser. Verified HTTP creation of LocalBot Verification and actual directory existence; verified native New Project and folder picker. Test project remains empty and available.
- Thor (builder), Athena (research), Zeus (review), Freya (testing), Hermes (assistant) have distinct prompts and explicit contact-vs-model identity. Idempotent migration preserves permissions, model assignments, memory and custom instructions. Historical message bodies remain unchanged. Runtime supplies the configured backend identity.
- 107 tests pass; signed clean source efd162f installed with 28 bundle hashes verified. Native project previews, attached Activity expansion, actual search output and corrected message layout checked. No broad goal loop resumed.

### 2026-09-17 — Compact terminal activity and native chrome
- Activity is attached below the message as a fixed 256×32 control. Native top-edge popover opens upward with a 480×280 vertically scrollable terminal transcript, real command/path/content/output/status, bounded output and active-state shimmer. Native screenshot verified upward opening and actual web-search output; no synthetic terminal events.
- Project rows are flat under manually animated folder headers, removing native disclosure indentation. Header/sidebar/empty-state avatar stacks use actual members (up to three); empty auto chats show a sparkle rather than invented contacts. Folder/archive/settings icons enlarged. Recents remain title-only.
- Cmd-plus/equal, Cmd-minus and Cmd-0 adjust persisted message/composer/code sizes (11–24, default14). Native message reflow and reset verified.
- Transparent full-size window chrome with native NSVisualEffectView preserves material behind controls; final fullscreen screenshot confirms the opaque gray strip is gone. Folder collapse/expand and conversation selection verified.
- Installed clean signed source 92c177b, all 28 bundle hashes verified. UI-only change: sequential Swift/package build passed; runtime tests not rerun here. User data preserved; runtime idle confirmed before each install.
- Separate user-owned session 01a0af33-905b-7f10-8c3e-6ac414afea1b owns codex/localbot-tool-parity. It reports119 tests and real streaming improvements; not merged into this UI release. Full vendor tool parity remains incomplete and is not claimed.

### 2026-09-18 — Research protocol compatibility and plain terminal UI
- Reproduced actual subscription CLI failure on the user's curriculum query: a successful search followed by `{type: "other"}` caused `Unsupported web search event`. Current generated app-server schema permits other, nullable action and nullable open/find metadata. Normalize these without treating unknown metadata as verified search evidence; malformed values, missing actual search and unsafe sources remain rejected. Same-query live search returned both semesters and official university sources after repair.
- Persist public `summaryTextDelta` summaries in separate run_events; never ingest raw reasoning/textDelta or reasoning content. Final throttled summary is saved, failed/interrupted generation closes the event, restart recovery marks pending events interrupted. Native typing remains during inference, not running tools. Require a short genuine plan before initial tool calls.
- Plain shared terminal transcript replaces inspector cards, includes thinking summaries and real tool output, starts short output at the top, streams verified web events as they arrive. Message attachment is centered and slightly tucked beneath the bubble with readable text. Inspector header constrained and shared background removes white top strip.
- Live end-to-end attempt completed two real searches, then encountered a separate transport disconnect while composing the final reply. Added one bounded decision-only reconnect; completed tool calls are not replayed. Fixture confirms unchanged evidence across retry.
- 110 sequential tests passed plus tool inventory gate; signed native builds verified. No broad goal or automation resumed; separate tool-parity branch remains separate.
- Original Documents checkout is iCloud dataless and reads stall; requested brctl hydration without deleting data. Worked from clean GitHub master clone at /Users/ibrahimsaitakarcesme/LocalBot-repair. Original checkout was not overwritten.
- Final live application follow-up completed with no errors: twelve first-year courses across two semesters, ECTS and official course catalogue/overview links returned in the user's existing research conversation. Streamed web events and public summaries observed through live Activity and native AX.
- Installed verified source d364c72 includes all runtime and terminal/centering fixes. Package b27b95b additionally constrains inspector-open title to180pt; installation deferred because two CUA close attempts were interrupted by user activity. Did not kill the native application or overwrite an in-use bundle.
## Tool parity branch — conversation metadata (2026-09-17)

Created isolated `codex/localbot-tool-parity`. Added project-scoped conversation discovery with bounded pagination/archive filtering and current-conversation renaming with Ask-mode approval, expected-title conflict detection, cancellation and durable metadata. Three focused tests pass, including engine approval with a concurrent user rename. No macOS source, installed application, user runtime database or Codex credentials accessed. Full inventory parity remains incomplete; 35 registered tools are not 35/269 coverage.

Tool parity checkpoint 2: `get_usage_limits` uses the official read-only Codex CLI account method through the provider interface, with no local-to-cloud fallback. Controlled adapter/engine tests pass, and a live CLI read returned validated windows without storing personal account values. ChatGPT credentials remain owned by CLI. Credit/reset fields and remote-host selection remain explicitly partial.

Inventory checkpoint: all 269 captured primary entries plus 13 controls now have explicit routes or blockers. The audit compares their names exactly with the captured catalog. Vendor family counts and authentication/engineering dependencies are recorded in docs/TOOL-PARITY-PLAN.md. No service stubs were registered or marked implemented.

Streaming checkpoint requested by the concurrent UI task: fixed dropped trailing terminal output, added UTF-8 stream decoding, forwarded Git output and preserved cancelled Activity output. Background sessions now publish bounded batched Activity snapshots without consuming process_poll output and stop publishing when ownership is released. Full sequential npm test:119/119 pass; catalog audit covers269+13 entries. Runtime emits its existing changed callback for each persisted batch; server connects this to revision/SSE notifications. No Swift edits or installed-app changes.

### 2026-09-18 — Native workspace panels and tools integration
- Integrated completed tool-parity commits cf336b7, 6972ea2, 41163f6 and 599dfcc while preserving current search protocol/public-summary fixes. 36 built-ins; external vendor/host parity remains incomplete. 123 sequential runtime tests and inventory gate passed.
- Native NSTextView composer handles Shift-Return as an actual newline at the selection; Return submits, IME marked text remains editable. Verified two lines without submission in installed UI.
- Two-axis native Activity transcript follows new output at the tail and retains the reader's position when inspecting older text. Confirmed horizontal scroller moves to 0.2. Recessed centered activity tabs now overlap bubbles by18pt with text clearance. Fullscreen toolbar remains visible; inspector header shares glass background with the upper region.
- Exclusive Activity/workspace right panel, animated width and resizable divider; stable equality checks avoid publishing unchanged snapshots/messages. Main conversation switches retain workspace tabs. Native PTY via pinned SwiftTerm, embedded WKWebView, scoped Files editor, real Git review, independent side-chat models, conversation drag payload/drop destinations and context-menu docking. Files save rejects external edits and out-of-project symlinks; three XCTest cases passed against real temporary files/Git repositories.
- Live installed UI verified PTY command output, correct project cwd and stty dimensions, Ctrl-C, HTTPS navigation/back, a message source link opening internally, project HTML opened from Files in embedded browser, and separate successful main/side responses in the same game3 project. Internal routing initially misinterpreted 'Do not use tools'; fixed the metadata/task distinction with a bounded missing-routing-call retry and regression test. Both chats subsequently completed with real subscription responses.
- Final installed source be001ab, clean signed package with33 hashes. Native client idle RSS about85MiB and runtime28MiB, both0%CPU at observation (excludes WebKit subprocesses). Source checkout remains /Users/ibrahimsaitakarcesme/LocalBot-repair; dataless original preserved.
- Physical drag/resize gesture verification was interrupted by desktop control/user-state changes. Drag/drop handlers implemented; no successful physical drag is claimed. Sidebar native toggle, final fullscreen rendering, Activity/workspace exclusivity and file navigation observed. Browser is a manual in-app workspace browser; this does not add an agent browser-automation tool or claim full Safari feature parity. No goal loop or automation started; history preserved.

### 2026-09-18 — Workspace glass redesign and performance pass
- Replaced the stacked Workspace header/tab bars with one inset native SwiftUI glass surface (macOS26; material fallback), a compact bottom dock, and contextual tool controls. Project conversations now use22pt colored bot portraits and title-only28pt rows like Recents. Original supplied artwork is packaged unchanged for the app icon; all Avatar instances render it with agent color.
- Only the selected tool pane is mounted. Browser/PTY instances and file draft state live in the tab; hidden side chats stop UI polling without stopping runtime tasks. Message width measurements are cached. Divider cursor uses AppKit cursor regions; panel buttons use native scoped pointer styles. Fixed native Menu image overflow using bounded Canvas rendering, narrow-window panel overflow and avatar gutter clipping after live screenshots.
- Activity is a lazy chronological execution feed: status, command/path/query, bounded preview, expandable two-axis output and explicit follow-live control. Uses only recorded events/provider public summaries. Large unexpanded outputs are not laid out in full.
- Performance test found a2MB single-line preview scanned the whole string:100 repeats4603ms before,3–5ms after bounding input before splitting.1000 unchanged native transcript updates0.15–0.24ms. These are specific microbenchmarks, not FPS/whole-app speed claims. Sampled native process footprint111.1MB before and69.0MB after under differing session histories; not a controlled memory comparison and excludes WebKit.
- Six native tests pass: retained file draft, bounded preview, unchanged transcript updates, stale-write protection, symlink boundary, staged/working Git diff. Live UI: HTTPS Example Domain, browser state preserved across terminal tab, real GLASS_TERMINAL_OK output, repeated panel toggles, long project activity, expandable run_tests command/output, compact project portrait row, normal and fullscreen glass containment. Final installed clean source5ea321a,35 signed bundle hashes verified. Original histories preserved.
- Physical divider drag remains unverified: CUA returned noWindowsAvailable. No claimed frame-rate guarantee or complete cursor hover matrix. No goal or automation resumed.

### 2026-09-18 — Message portraits and workspace detail pass
- Imported the user's other task's transparent native SwiftUI mascot kit; mapped agent colors to its palettes. Static historical portraits avoid recurring animation work; current run portraits use thinking/working/approval phases and recent completion uses its own message task. Reduced the small active mascot timeline to30fps; Reduce Motion pauses it. No video decoding dependency.
- Right controls now live inside the glass when open and return to the toolbar when closed. Conversation title uses leading toolbar placement and horizontal padding. Terminal header/content have explicit insets.
- Each assistant message has a top-aligned transparent portrait and a full-response clipboard button. Activity measures actual bubble width, uses90% centered underneath with the retained recess/shimmer/popover, and names the tool action with a matching symbol. Team preparation uses a shimmering sparkles icon.
- Six native tests pass. Installed UI checked title placement, open/closed controls, transparent per-message portraits, terminal margins, contextual activity and expanded output. Copy button pasted the complete selected response into an empty composer; cleared the test draft without sending. Idle observation: native0%CPU/~87MiB RSS; runtime0%CPU/~28MiB RSS, excluding WebKit. This is an idle sample, not an FPS benchmark. Fullscreen shortcut did not change the observed window; no new fullscreen or exhaustive motion-state visual claim.

### 2026-09-18 — Sidebar top-edge consistency
- Right glass surface now ignores only the top container safe area with an8pt outer inset, aligning its controls with the titlebar and removing the dead band. Collapsed toolbar controls have12pt horizontal interior clearance and fixed intrinsic sizing.
- Native toolbar stays transparent so it cannot cover the right controls. A hit-test-transparent ultra-thin material overlay across the conversation title region fades at its bottom edge. Verified installed open/closed workspace using screenshots and actual toggle buttons. Build/signature verified,35 bundle hashes, source717d106.
- Existing research runtime was left running while checking the replacement client. Installed only after no active tasks remained. The existing PDF task independently ended with a JSON parsing error; this UI-only change does not repair that runtime failure.

### 2026-09-18 — Continuous window glass backing
- Replaced conversation-width blur with one full-window backdrop shared beneath native sidebar, conversation and workspace. Restricted the transcript's opaque backing to its own safe-area bounds so it no longer overwrites the titlebar material.
- Preserved macOS26 native regular Liquid Glass; refined the right surface with a0.5pt directional edge highlight and subtle shadow. Reduce Transparency uses an opaque system surface. No custom animation loop or shader dependency.
- Clean signed build9f99959 installed,35 file hashes verified. Native screenshots checked both sidebars open and left collapse/reopen; common upper backing no longer changes at the column edges. Existing failed PDF task/history retained.
